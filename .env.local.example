# Turso — use a token scoped to this database only (read+write), not an account-wide token,
# since it ships to the browser for client-direct DB access.
NEXT_PUBLIC_TURSO_DATABASE_URL=libsql://<db-name>-<org>.turso.io
NEXT_PUBLIC_TURSO_AUTH_TOKEN=
